
=================================================================================================================
OPEN ISSUES
=================================================================================================================

setup: no initial users are created in the scripts
setup: small device tree change to make uart work, in one note docs

=================================================================================================================
PATCHES
=================================================================================================================
added to post install                            -> groups: missing nsadmin and nsusers
fixed paths                                      -> firewalld: networking path is wrong / missing
added to makefile                                -> systemd: Interactive authenticated required - probably missing 10-systemd1.rules
added chmod                                      -> permissions: ns exe is available on all users
added to makefile                                -> systemd: snmp: disable seemed to have no effect, enabled shows when disabled even though switch shows correct state
added exception to python Systemd Stop and Start -> snmp: the enable / disable error is not getting passed back to the scrreen when a simple user
i think this is false, no fix needed             -> network manager: nsuser was able to edit the ip address... ???? not sure if this is true needs more testing
updated to one error, added canOpenFunc          -> network manager: editing ipv4 shows two errors when editing dns servers (adding 8.8.8.8)
removed broken package                           -> note: ns-admin 2.0.10 is broken... remove... will need to test going from ns2 to ns-admin
added disable command to ns-admin post install   -> setup: disable systemd-networkd so that the /proc/net file only has 1 address
added disable command to ns-serial pre install   -> setup: disable variscite-bt
passed error back up from systemstop and start   -> firewalld: Service Switch does not indicate error for nsuser
added isAdmin block to the zone navigation...    -> firewalld: Add zone dialog does not indicate error or close when normal user
fixed a some calls making session ids            -> login: the session was not terminated as expected when logging on with a different user... 
attempted to cache these                         -> firewalld page: list loads slowly see about pulling in list before hand
attempted to cache these                         -> system page: list loads slowly see about pulling in list before hand

removed some of the dependences from the package -> boot loop: found that I incorrectly setup the services to wait for each other
removed some of the dependences from the package -> boot loop: found that installing network manager caused a reboot. 
removed some of the dependences from the package -> boot loop: pre installing network manager, nginx and firewalld

moved the id to the browser id                   -> login: logging in does not remove other logged in users
this seems like a good thing:                    -> login: when logging in from another pc the user on the other pc was logged out
i think this will be resolved with new id        -> storage: the general storage, used for checking if users can open something, was not in the correct state because of the login issue ^^^^^
addressed with default baud rate set             -> serial: on reboot the baud rate of the serial port was reset
checked firewalld before call                    -> firewalld: found that if firewalld is disabled when frontend starts it fails because the dbus calls do not return correctly....
removed cli endpoints, ran others through polkit -> ns exe permissions: user1234 in nsuser was still able to be executed.... part of the problem is they need it.... may add polkit check to ns?
calling ui.navigate.reload() instead of navigate -> pages: missing a sub page on logout.... 

=================================================================================================================
EXCEPTIONS
=================================================================================================================





=================================================================================================================
SETUP INFO / PROCESS
=================================================================================================================
connect to variscite serial port
touch ND2310
hostname -I

uname -a: Linux imx8mn-var-som 6.6.52-var-lts-next-ge71e2277b289-dirty #1 SMP PREEMPT Tue Feb 17 10:52:56 CST 2026 aarch64 GNU/Linux


ssh in to run:

sudo systemctl disable variscite-bt.service
sudo systemctl disable variscite-wifi.service
sudo reboot now -- no issues so far... 

sudo apt install tuptime -- no issues

sudo apt update -- no issues
sudo apt install network-manager -- no issues
sudo systemctl disable NetworkManager-wait-online.service -- no issues
sudo reboot now -- no issues
sudo apt install nginx -- no issues

sudo dpkg-reconfigure locales -> en_US.UTF-8 UTF-8
sudo reboot now -- no issues.... amazing
sudo apt install firewalld -- no issues

sudo apt install snmpd -- no issues
sudo dpkg -i ./ns-serial-mux_2.0.18_arm64.deb
sudo dpkg -i ./ns-agent_2.0.11_arm64.deb
sudo apt install socat
socat - UNIX-CONNECT:/var/lib/ns/serial.sock -- worked...
sudo reboot now -- no issues.....
htop -- no issues
sudo dpkg --no-act -i ns-admin_2.0.30_arm64.deb -- no issues
sudo dpkg -i ns-admin_2.0.30_arm64.deb -- ssh session seemed to lock up. serial session available still. in serial, logged in and ran top. Saw that ns-serial was taking up a bunch but not crazy. ssh session still not back.... 
access via https://10.1.10.154 was available. signed in via root and root. Still no ssh session back yet... but also no reboots yet.. ran control c in the ssh and got the session back????

watched htop for a while... on the serial string status page of ns-admin
clicked to system, network... rest of pages... while watching htop.. no issues yet. 

sudo reboot now -- no issues yet
ns account add admin admin -a -- makes admin account.... 
logged in with admin and watched htop and status string page...